import type { PointTrack } from "@subpixel/contracts";

export function TrackTable({ tracks, onReview }: { tracks: PointTrack[]; onReview: (frame: number) => void }) { return <div className="table-wrap"><table><thead><tr><th>帧</th><th>X / px</th><th>Y / px</th><th>置信度</th><th>状态</th><th /></tr></thead><tbody>{tracks.slice(-12).reverse().map(track => <tr key={track.frame} className={track.state === "suspect" || track.state === "lost" ? "row-warning" : ""}><td>{track.frame}</td><td>{track.x.toFixed(3)}</td><td>{track.y.toFixed(3)}</td><td>{Math.round(track.confidence * 100)}%</td><td><span className={`state state-${track.state}`}>{track.state}</span></td><td>{track.state !== "valid" && <button className="text-button" onClick={() => onReview(track.frame)}>复核</button>}</td></tr>)}</tbody></table></div>; }
